# Master Chat — Web test build

Built artefacts only. The source lives in the private `masterchat` repository.

Live: https://mexx-music.github.io/masterchat-web/

Built from source commit `f71e3e7`. No AI key is compiled in, so this
build runs the local Essentials offline, and the phone list is seeded with a
**fictional** starter list — the real one never leaves the private repository.
